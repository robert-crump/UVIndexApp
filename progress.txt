## 2026-09-19 — Issue #42: Settings — view model owns user intent, screen is presentation only

### Status: COMPLETE (manual on-device check still to be done: change skin type -> self-protection widget updates; toggle daily off/on -> no duplicate schedules)

### What was implemented
- New `ui/settings/SettingsViewModel.kt`: `SettingsUiState` (skin type, daily/UV Warning flags, background-location granted),
  intents `setSkinType` (persist + widget refresh), `setDailyEnabled`, `setUvWarningEnabled`, `requestBackgroundLocation`,
  `refreshPermissions`, permission-result callbacks. Steps only an Activity can do come out as `PermissionAction` events.
- `SkinTypeStore` interface (implemented by `DataStoreManager`); `LocationPermissionState` + `AppPermissions.hasBackgroundLocation`
  in the permission module. Daily toggle needs no schedule call: the tick worker reads the flag (#39).
- `SettingsScreen` moved to `ui/settings/SettingsScreen.kt` (192 lines) with `SettingsSections.kt`; constructs no stores/schedulers.
  `SettingsActivity` builds the VM via `SettingsViewModel.factory`.
- `SettingsViewModelTest` with fake skin-type, history and permission stores. `testDebugUnitTest` and `assembleDebug` pass.

## 2026-09-19 — Issue #41: Widgets — remaining four widgets become pure binders, one placeholder and error color

### Status: COMPLETE (manual on-device check of all five widgets, and of the placeholder after clearing app storage, still to be done)

### What was implemented
- New pure binders `HourlyUvBinder`, `WindBinder`, `AirQualityBinder`, `SelfProtectionBinder`; shared `WidgetBinding.kt`
  holds `WidgetTone`, `WidgetTapTarget` (MainActivity, Settings), `WIDGET_PLACEHOLDER` ("–") and `WIDGET_ERROR_COLOR`.
- `WidgetHost.updateFromReceiver(context, WidgetKind, pendingResult)` loads the cached forecast (and skin type for the
  self-protection widget) once, binds and applies. All five providers are thin `onUpdate` shells; no `onReceive`
  re-dispatch of `ACTION_APPWIDGET_UPDATE`, no repository or coroutine scope outside the host.
- `UVForecast.remainingHours` (derived, not serialized) feeds the 4x1 binder. The 4x1 refresh action now calls
  `WidgetHost.refreshNow` -> `BackgroundSchedule.enqueueImmediateRefresh` (a TickWorker run with `force_fresh`).
- Tests: `WidgetBindersTest` (hourly 4+/<4 hours/end cells/null, wind, AQI, self-protection incl. no skin type and UV<1).
  `testDebugUnitTest` and `assembleDebug` pass.
- Note: `ScreenUnlockReceiver` (not a provider) still owns a coroutine scope for boot scheduling.

## 2026-09-19 — Issue #40: Fetch policy — callers state fetch intent only

### Status: COMPLETE (manual check still to be done: reopen within TTL -> cache read in log; move 20 km -> network fetch)

### What was implemented
- New `data/repository/FetchIntent.kt` (Fresh, FreshIfStale, CachedOnly). `WeatherRepository.getUVForecast(intent)` is the
  only read API; `forceRefresh`, `getCachedForecastForWidget` and `isCacheStale` are gone. CachedOnly never touches
  network or location.
- `schedule/FetchIntentRule.kt`: pure `fetchIntentFor(LocalTime)` -> Fresh at minute 0 of 06..17, else FreshIfStale;
  `FetchIntentTest` covers 05:59, 06:00, 17:00, 18:00, minute 0 vs 30.
- `TickWorker` passes the intent straight through. `MainViewModel`: FreshIfStale on initial load and resume, Fresh on
  pull-to-refresh; five-minute timer removed. Widget providers use CachedOnly; widget refresh button uses Fresh.
- `testDebugUnitTest` passes.

## 2026-09-19 — Issue #39: Background schedule — single tick worker replaces three workers

### Status: COMPLETE (manual one-morning check of a single 06:30 daily notification still to be done on device)

### What was implemented
- New `worker/TickWorker.kt`: the only `CoroutineWorker`. Per tick: `fetchIntentFor(now)`, repository forecast,
  `NotificationDecider.decide`, dispatch + record, then `WidgetUpdateHelper.updateAllWidgets`.
- New `schedule/FetchIntent.kt`: pure `fetchIntentFor(LocalTime)` (minute 0 in 6..17 -> Fresh, minute 0 otherwise ->
  FreshIfStale, else Cached); `FetchIntentTest`.
- Deleted `DailyUVCheckWorker`, `HourlyUpdateWorker`, `WidgetUpdateWorker`. `BackgroundSchedule.ensureScheduled` now
  registers only the 30 min periodic tick and the midnight one-shot, and cancels the four legacy work names.
  Removed `refreshWidgetsNow`; onResume and boot push the cache via `WidgetUpdateHelper.updateAllWidgets`.
- Settings daily toggle only writes the flag (no scheduling).
- Decider morning window is now `LocalTime` 06:30 (inclusive) to 12:00 (exclusive); tests added for 06:29, 06:30,
  11:59, 12:00. `testDebugUnitTest` passes.

## 2026-09-19 — Issue #38: 1x1 current-UV widget becomes a pure binder (tracer bullet)

### Status: COMPLETE (manual on-device check of value, color, tap still to be done)

### What was implemented
- New `widget/CurrentUvBinder.kt`: pure `bindCurrentUv(UVForecast?)` -> `CurrentUvBinding` (value text, time text,
  `WidgetTone` Risk(UvRisk)/Neutral, `WidgetTapTarget`). No Context/RemoteViews.
- New `widget/WidgetHost.kt`: loads the cached forecast once, applies the binding to RemoteViews (colors, background,
  tap intent) and updates all placed widgets; `goAsync` variant for receivers.
- `UVWidgetCurrent` is now a thin shell (only `onUpdate`); no repository, no coroutine scope, no `onReceive`
  re-dispatch; removed unused `ACTION_REFRESH_CURRENT`. Worker and unlock still go via `updateAllWidgets` broadcast,
  so the widget refreshes once per trigger. Other four providers unchanged.
- Tests: `CurrentUvBinderTest` (UV 0/3/6/8, null forecast, tap target). `testDebugUnitTest` passes.
